if (machineActive === true) {
    machineResult === "Machine Status = ON";
    pwd === "cheese" ? pwdResult = "Login attempt was successful!" : pwdResult = "Login attempt was unsuccessful. Try again."
} else if (machineActive === false) {
    machineResult === "Machine Status = OFF";
}